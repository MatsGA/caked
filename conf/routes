# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index
GET     /cakes                      controllers.Application.cakes
GET     /gallery                    controllers.Application.gallery
GET     /about                      controllers.Application.about
GET     /contact                    controllers.Application.contact
GET     /calendar                   controllers.Application.calendar
GET     /language                   controllers.Application.setLanguage(lang: String ?= "no")
GET     /tweeted                    controllers.Application.tweeted

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
